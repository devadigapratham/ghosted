# macOS
.DS_Store

# Node (no runtime deps — this is only for the test/icon tooling)
node_modules/
npm-debug.log*
package-lock.json

# Local editor and tooling settings
.claude/

# Packaged extension builds
*.zip
*.crx
*.pem
